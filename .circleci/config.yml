version: 2
jobs:
  lint:
    docker:
      - image: ruby:2.2.6
    steps:
      - checkout
      - run: bundle install
      - run: rake lint
  validate:
    docker:
      - image: ruby:2.2.6
    steps:
      - checkout
      - run: bundle install
      - run: rake validate
